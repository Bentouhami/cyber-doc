# Git workflow summary

Purpose: follow the repository’s approved Git split/backup workflow safely.

Key rules:

- Use the documented split/backup process.
- Prefer read-only checks first.
- Avoid destructive commands unless explicitly approved.
- Keep output minimal.
