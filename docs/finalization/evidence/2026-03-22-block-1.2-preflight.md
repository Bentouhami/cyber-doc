# Preflight - Block 1.2 Templates Management (CRUD + Import)

Date: 2026-03-22  
Scope: `Admin > Templates management`

## 1) Current State Found

Status: **Partially implemented (advanced)**

Already present:
- Admin pages:
  - `/admin/templates`
  - `/admin/templates/new`
  - `/admin/templates/import`
  - `/admin/templates/requests`
- Rich `TemplateList` with search/filter, bulk actions, duplicate/archive/review actions.
- Create form supports assisted mode + developer mode + generated HTML preview.
- Import form supports JSON and Word draft request path.
- API routes for create/update/import/duplicate/asset/taxonomy already in place.
- Duplicate candidate guard appears integrated in create/import flows.

Potential gaps to validate in runtime:
- End-to-end create/import success paths with current auth/session.
- Consistency of all admin labels in FR/AR under real UI navigation.
- Error harmonization (some fallbacks may still return English generic strings like "Import failed").

## 2) Reuse Plan (DRY)

Reused as-is:
- Existing API routes and services (`templateImportService`, duplicate service).
- Existing component stack (`TemplateList`, `TemplateCreateForm`, `TemplateImportForm`).
- Existing i18n namespace `templates`.

No new architecture layer needed.

## 3) Minimal Change Plan (KISS)

- Focus only on runtime UX/error/i18n polish where needed.
- Do not replace core template architecture.
- Keep API contracts stable unless a strict bug requires change.

## 4) Impacted Files (expected, if fixes are needed)

- `components/admin/templates/template-list.tsx`
- `components/admin/templates/template-create-form.tsx`
- `components/admin/templates/template-import-form.tsx`
- `locales/fr/common.json`
- `locales/ar/common.json`

## 5) Validation plan for this block

- Admin smoke runner (routes + CRUD/import/duplicate guard)
- Manual checks on `/admin/templates*`
- `npm run lint`
- `npm run type-check`
- `npm run build`
- `npm run i18n:audit`
