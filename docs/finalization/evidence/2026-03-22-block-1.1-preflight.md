# Preflight - Block 1.1 Employee Management (CRUD)

Date: 2026-03-22  
Scope: `Admin > Employee management (CRUD)`

## 1) Current State Found

Status: **Partially implemented (advanced)**

Already present:
- Admin employees page with list/search/filter/status badges.
- Create/Edit modal with `EmployeeForm` and Zod validations.
- API endpoints:
  - `GET/POST /api/users`
  - `GET/PATCH/DELETE /api/users/[userId]`
  - `GET /api/users/me`
- Guardrails:
  - cannot delete own account,
  - cannot deactivate own account,
  - cannot remove own admin role.
- i18n coverage FR/AR for most labels.
- Existing smoke runner includes employee CRUD API flow.

Gap identified:
- Some API error messages were still surfaced as raw English text in UI.

## 2) Reuse Plan (DRY)

Reused as-is:
- Existing API routes and business logic.
- Existing form architecture and DTO mapper.
- Existing i18n structure (`employeeForm`, `employeeStatus`).

No new endpoint, no new component tree, no new dependency.

## 3) Minimal Change Plan (KISS)

- Add a local API-error mapper in `EmployeeForm`.
- Add only missing i18n keys for those mapped errors.
- Keep server contracts untouched.

## 4) Impacted Files

- `components/admin/employee-form.tsx`
- `locales/fr/common.json`
- `locales/ar/common.json`

## 5) Validation Run

Executed and passing:
- `npm run lint`
- `npm run type-check`
- `npm run i18n:audit`
- admin smoke runner (`tmp/admin-smoke/results.json`) with:
  - Employees CRUD create/update/delete: PASS
  - Admin routes render checks: PASS

## 6) Risks / Notes

- Manual UI smoke still required to confirm full CRUD UX behavior in browser for this branch state.
- Repo currently contains many unrelated in-progress changes; no reset/revert performed.
